openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout ./traefik/certs/local.key -out ./traefik/certs/local.crt -subj "/CN=*.local" && echo "Certificate and key generated successfully." && echo "You can now use the generated certificate and key for your Traefik setup." && echo "Remember to configure Traefik to use these files." && echo "If you encounter any issues, please check the logs for more information." && echo "Make sure to secure your key and certificate files appropriately."